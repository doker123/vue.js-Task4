# Интернет-магазин «Просто купить»

Веб-приложение интернет-магазина, разработанное на **Vue.js 3** с использованием **Vuex** для управления состоянием и **Vue Router** для навигации.

## 📋 Описание

Приложение предоставляет полный функционал интернет-магазина:
- Просмотр каталога товаров
- Регистрация и авторизация пользователей
- Добавление товаров в корзину
- Оформление заказов
- Просмотр истории заказов

## 🛠 Технологии

- **Vue.js 3** — прогрессивный фреймворк для создания пользовательских интерфейсов
- **Vuex 4** — централизованное хранилище данных
- **Vue Router 4** — официальный маршрутизатор для Vue.js
- **Vue CLI 5** — инструмент сборки и разработки

## 🚀 Быстрый старт

### Требования

- Node.js (версия 14.x или выше)
- npm (версия 6.x или выше)

### Установка

1. Клонируйте репозиторий или перейдите в директорию проекта:
```bash
cd vue.js-Task4
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения (файл `.env` уже содержит настройки по умолчанию):
```
VUE_APP_API_URL=http://lifestealer86.ru/api-shop
```

4. Запустите проект в режиме разработки:
```bash
npm run serve
```

Приложение будет доступно по адресу: `http://localhost:8080`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы появятся в папке `dist/`.

## 📁 Структура проекта

```
src/
├── main.js                 # Точка входа приложения
├── App.vue                 # Корневой компонент
├── router/
│   └── index.js            # Настройка маршрутизации
├── store/
│   └── index.js            # Vuex хранилище
├── utils/
│   └── api.js              # API клиент для взаимодействия с бэкендом
├── components/
│   ├── common_components/
│   │   └── Header.vue      # Шапка сайта
│   ├── auth/
│   │   ├── Login.vue       # Форма входа
│   │   ├── Logout.vue      # Компонент выхода
│   │   └── Registration.vue # Форма регистрации
│   └── products/
│       ├── Cart.vue        # Корзина товаров
│       ├── Orders.vue      # Список заказов
│       └── Product.vue     # Карточка товара
└── views/
    ├── HomeView.vue        # Главная страница (каталог)
    ├── LoginView.vue       # Страница входа
    ├── RegisterView.vue    # Страница регистрации
    ├── CartView.vue        # Страница корзины
    └── OrdersView.vue      # Страница заказов
```

## 🎯 Основные возможности

### Каталог товаров
- Отображение списка товаров с изображениями
- Фильтрация и поиск (при наличии на бэкенде)
- Добавление товаров в корзину (требуется авторизация)

### Авторизация и регистрация
- Регистрация нового пользователя с валидацией полей
- Вход по email и паролю
- Защита маршрутов (требуется авторизация для корзины и заказов)
- Перенаправление неавторизованных пользователей

### Корзина
- Добавление/удаление товаров
- Изменение количества товаров
- Подсчёт общей стоимости
- Оформление заказа

### Заказы
- Просмотр истории заказов
- Отображение статуса заказа
- Детализация состава заказа

## 🔐 Маршруты

| Маршрут | Описание | Доступ |
|---------|----------|--------|
| `/` | Каталог товаров | Все |
| `/login` | Страница входа | Только гости |
| `/register` | Страница регистрации | Только гости |
| `/cart` | Корзина | Только авторизованные |
| `/orders` | История заказов | Только авторизованные |

## 📦 Управление состоянием (Vuex)

### State
- `token` — JWT токен авторизации
- `user` — данные пользователя
- `products` — список товаров
- `cart` — товары в корзине
- `orders` — история заказов
- `loading` — флаг загрузки
- `error` — ошибка

### Getters
- `isAuthenticated` — проверка авторизации
- `products` — получение списка товаров
- `cart` — получение корзины
- `cartTotal` — общая сумма корзины
- `cartCount` — количество товаров в корзине
- `orders` — получение заказов

### Actions
- `login` — авторизация пользователя
- `logout` — выход из системы
- `register` — регистрация пользователя
- `loadProducts` — загрузка каталога товаров
- `addToCart` — добавление товара в корзину
- `removeFromCart` — удаление товара из корзины
- `updateCartQuantity` — изменение количества товара
- `createOrder` — оформление заказа
- `loadOrders` — загрузка истории заказов
- `loadCart` — загрузка корзины

## 🎨 Стилизация

Приложение использует собственные CSS-стили с:
- Градиентными фонами
- Анимациями и переходами
- Адаптивным дизайном (mobile-first)
- Тенями и скруглениями в стиле Material Design

## 📱 Адаптивность

Приложение полностью адаптировано для мобильных устройств:
- Гибкая сетка товаров
- Адаптивная навигация
- Оптимизированные формы

## 🔌 API

Приложение взаимодействует с внешним API по адресу: `http://lifestealer86.ru/api-shop`

### Основные эндпоинты:
- `POST /login` — авторизация
- `POST /signup` — регистрация
- `GET /products` — получение списка товаров
- `GET /cart` — получение корзины
- `POST /cart/:productId` — добавление в корзину
- `DELETE /cart/:cartItemId` — удаление из корзины
- `POST /order` — создание заказа
- `GET /order` — получение истории заказов

## 📝 Лицензия

Проект создан в учебных целях.
